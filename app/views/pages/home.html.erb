<main class="container">
  <div class="header">
    <h1>Quote editor</h1>
  </div>
  <p>A blazing fast quote editor with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View quotes", quotes_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
  <% end %>
</main>
